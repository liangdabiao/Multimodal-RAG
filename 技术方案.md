● 方案要点：

  - 项目结构：模块化设计，core/ 放核心逻辑（embedder、vector_store、retriever、generator），utils/ 放工具函数，app.py 做
   Gradio UI
  - 多文档支持：单集合 + doc_name 字段区分不同 PDF，搜索时可选择特定文档或全部
  - 云 Milvus：直接用你 .env 中配置的 Zilliz Serverless 连接，IVF_FLAT 索引
  - GPU 兼容：自动检测显存，4GB RTX 3050 不够加载 4.4GB 模型时会自动回退 CPU
  - 懒加载：ColQwen2 首次使用时才加载，UI 启动不等待
  - 总计约 12 个新文件，预计代码量 ~1000 行

● User approved Claude's plan
  ⎿  Plan saved to: C:\Users\49707\.claude\plans\nested-jingling-wolf.md · /plan to edit
     Multimodal RAG - PDF 智能问答系统 实现计划

     Context

     基于文章教程，构建一个完整的多模态 RAG 系统：PDF 页面 → ColQwen2 编码为 patch 向量 → Zilliz 云向量库 → MaxSim 检索
     → Qwen3.5 多模态 LLM 生成回答。用户选择了 Gradio Web UI + OpenRouter API + 云 Milvus (Zilliz Serverless)。

     项目结构

     D:/PDF-AI/
     ├── .env                    # 已有，密钥配置
     ├── .env.example            # 已有
     ├── .gitignore              # 新建
     ├── requirements.txt        # 新建
     ├── app.py                  # Gradio Web UI 入口
     ├── config.py               # 配置中心，从 .env 加载
     ├── core/
     │   ├── __init__.py
     │   ├── embedder.py         # ColQwen2 模型封装（加载、编码图片、编码查询）
     │   ├── vector_store.py     # Milvus/Zilliz 连接、集合管理、向量搜索
     │   ├── retriever.py        # MaxSim 检索逻辑（查询 → Top-K 页面）
     │   └── generator.py        # OpenRouter LLM 回答生成
     ├── utils/
     │   ├── __init__.py
     │   ├── pdf_processor.py    # PDF → PIL Images (pdf2image)
     │   └── image_utils.py      # image_to_uri、缩放辅助
     └── models/                 # 本地模型缓存目录
         └── .gitkeep

     核心模块设计

     1. config.py — 配置中心

     - 从 .env 加载所有配置（Milvus 连接、API Key、模型路径等）
     - 自动检测 CUDA/VRAM，VRAM < 6GB 时默认 CPU（避免 OOM）
     - 自动检测 flash-attn 是否可用
     - 关键配置项：embed_model_path、embed_dim(128)、device、milvus_uri/token、collection_name、index_type(IVF_FLAT)、to
     p_k(3)、candidate_patches(300)、openrouter_api_key、generation_model

     2. core/embedder.py — ColQwen2 封装

     - ColQwen2Embedder 类
     - encode_images(images) → 每页产出 ~755 个 128 维 patch 向量，batch_size=2（GPU）/1（CPU）
     - encode_query(text) → 查询编码为 token 向量
     - 自动处理 device/dtype/flash_attn

     3. core/vector_store.py — Zilliz 向量库

     - VectorStore 类，连接 Zilliz Serverless
     - 集合 Schema：id(自增主键)、doc_name(VARCHAR, 文件名)、page_idx(INT64,
     页码)、patch_idx(INT64)、vector(FLOAT_VECTOR 128)
     - 索引：IVF_FLAT, metric_type=IP, nlist=1024
     - 多文档支持：通过 doc_name 字段区分不同 PDF，搜索时可按 doc_name 过滤

     4. core/retriever.py — MaxSim 检索

     - MaxSimRetriever 类，组合 embedder + vector_store
     - MaxSim 算法：每个 query token 向量搜索 Top-300 patch → 按 (doc_name, page_idx) 聚合最大匹配分数 →
     求和得到文档总分 → 排序取 Top-K
     - 返回 RetrievalResult(doc_name, page_idx, score) 列表

     5. core/generator.py — LLM 生成

     - AnswerGenerator 类，通过 OpenAI SDK 调用 OpenRouter
     - 图片转 base64 data URI（最大 1600px）
     - 构建多模态 prompt 发送给 qwen/qwen3.5-397b-a17b

     6. utils/pdf_processor.py — PDF 处理

     - pdf_to_images(pdf_path, dpi=150) → PIL Image 列表
     - Windows poppler 检测和友好错误提示

     7. app.py — Gradio Web UI

     - 文档管理区：上传 PDF、选择文档下拉框、编码按钮、清空按钮
     - 提问区：问题输入框、搜索按钮
     - 结果区：检索到的页面图片 Gallery（带文档名、页码、分数标签）、Markdown 回答
     - 懒加载：ColQwen2 模型首次使用时才加载，避免启动等待
     - 进度反馈：编码和搜索过程中实时显示进度

     数据流

     文档入库流程

     上传 PDF → pdf_to_images (150 DPI) → ColQwen2 编码每页 → patch 向量写入 Zilliz → 缓存页面图片到内存

     查询流程

     用户提问 → ColQwen2 编码查询 → 逐 token 搜索 Zilliz → MaxSim 聚合 → 取 Top-K 页面图片 → 图片+问题发给 Qwen3.5 →
     返回回答

     关键技术决策

     ┌──────────────┬──────────────────────────┬──────────────────────────────┐
     │     决策     │           选择           │             原因             │
     ├──────────────┼──────────────────────────┼──────────────────────────────┤
     │ 多文档存储   │ 单集合 + doc_name 字段   │ 简单，支持跨文档搜索         │
     ├──────────────┼──────────────────────────┼──────────────────────────────┤
     │ 模型加载     │ 懒加载 + gr.State 缓存   │ 避免 UI 启动慢               │
     ├──────────────┼──────────────────────────┼──────────────────────────────┤
     │ GPU 显存不足 │ 自动回退 CPU             │ RTX 3050 仅 4GB，模型 ~4.4GB │
     ├──────────────┼──────────────────────────┼──────────────────────────────┤
     │ flash-attn   │ 可选，不可用时回退       │ Windows 难以安装             │
     ├──────────────┼──────────────────────────┼──────────────────────────────┤
     │ Zilliz 连接  │ MilvusClient(uri, token) │ Serverless 模式直接 HTTPS    │
     └──────────────┴──────────────────────────┴──────────────────────────────┘

     实现顺序

     1. 基础设施：.gitignore、requirements.txt、config.py
     2. 工具模块：utils/pdf_processor.py、utils/image_utils.py
     3. 向量存储：core/vector_store.py（可独立测试）
     4. ML 组件：core/embedder.py → core/retriever.py → core/generator.py
     5. Web UI：app.py（组合所有模块）
     6. 验证：端到端测试（上传 PDF → 编码 → 提问 → 查看结果）

     验证方法

     1. 启动 python app.py，确认 Gradio UI 正常加载
     2. 上传一个测试 PDF，点击编码，确认 Zilliz 中数据写入成功
     3. 输入问题，确认检索到相关页面并在 Gallery 中显示
     4. 确认 LLM 返回准确回答
     5. 测试多文档场景：上传第二个 PDF，确认可切换搜索范围





    方案：用 Cohere embed-v4.0 API 替换本地 ColQwen2

     Context

     当前项目依赖 PyTorch + ColQwen2 本地运行（模型 ~4.4GB），安装慢、需要 poppler、吃显存。改为使用 Cohere embed-v4.0
     云端 API，彻底去掉 PyTorch 和 poppler 依赖。

     Cohere embed-v4.0 关键信息

     - 图片输入：base64 data URI（PNG/JPEG），最大 5MB
     - 批量：每页一张图，一次请求可批量最多 96 张
     - input_type：存文档用 search_document，查问题用 search_query
     - 向量维度：默认 1536，可通过 output_dimension 设为 1024/512/256
     - 返回：每张图一个向量（单向量，不再是 ColBERT 多向量）
     - Python SDK：cohere 包，ClientV2 或 Client 均可

     改动文件清单

     1. requirements.txt — 大幅精简

     删除：torch、torchvision、colpali-engine、pdf2image
     新增：cohere、PyMuPDF（轻量 PDF 转图片，替代 pdf2image+poppler）

     最终：
     cohere>=5.0.0
     pymilvus>=2.4.0
     openai>=1.0.0
     PyMuPDF>=1.24.0
     pillow>=10.0.0
     gradio>=4.0.0
     numpy>=1.24.0
     python-dotenv>=1.0.0

     2. config.py — 调整参数

     - 删除：embed_model_path、device、dtype、encode_batch_size、poppler_path（GPU/CPU/flash-attn 检测全删）
     - 新增：COHERE_API_KEY、embed_model="embed-v4.0"、embed_dim=1024、cohere_batch_size=96
     - 保留：Milvus 配置、检索参数（top_k/candidate_patches → candidate_patches 改名为无意义，删除）、LLM
     配置、pdf_dpi、max_image_size

     3. utils/pdf_processor.py — 用 PyMuPDF 替代 pdf2image

     - 删除 pdf2image 依赖和 poppler 相关逻辑
     - 改用 fitz（PyMuPDF）将 PDF 每页渲染为 PIL Image
     - 无需 poppler，跨平台直接可用

     4. core/embedder.py — 从 ColQwen2 改为 Cohere API

     - 删除 torch/colpali_engine 导入
     - 新建 CohereEmbedder 类：
       - __init__：初始化 cohere.ClientV2(api_key)
       - encode_images(images: list[PIL.Image]) -> list[list[float]]：将图片转 base64 data URI，调用
     co.embed(images=[...], model="embed-v4.0", input_type="search_document", embedding_types=["float"],
     output_dimension=1024)，返回每页一个向量
       - encode_query(text: str) -> list[float]：调用 co.embed(texts=[text], model="embed-v4.0",
     input_type="search_query", embedding_types=["float"], output_dimension=1024)，返回单个向量
       - 支持批量处理（最多 96 张图/次）

     5. core/vector_store.py — 简化 schema

     - embed_dim 从 128 改为 1024
     - 每页只存 1 条 向量记录（不再是 ~755 条 patch）
     - Schema 不变：id、doc_name、page_idx、patch_idx（可保留字段但 patch_idx 恒为 0，或删除该字段）
     - 搜索逻辑不变（标准余弦/IP 搜索，不再需要 MaxSim）

     6. core/retriever.py — 大幅简化

     - 删除 MaxSim 多向量聚合逻辑
     - 改为标准单向量检索：编码查询 → 一次搜索 Zilliz → 返回 Top-K 页面
     - retrieve() 方法简化为：embedder.encode_query() → vector_store.search() → 排序取 Top-K

     7. app.py — 小幅调整

     - get_components() 中 ColQwen2Embedder 改为 CohereEmbedder
     - 删除 GPU 相关代码和 torch 导入
     - 进度条逻辑简化（不再有模型加载延迟）

     8. utils/image_utils.py — 保留

     - image_to_data_uri 仍然需要（Cohere 也用 base64 data URI）
     - max_size 可调小到 1200（Cohere 限制 5MB）

     不改动的文件

     - .env / .env.example：新增 COHERE_API_KEY 字段
     - core/generator.py：不动（OpenRouter + Qwen3.5 不变）

     性能对比

     ┌───────────────┬────────────────────────────┬────────────────────┐
     │     指标      │   改前（本地 ColQwen2）    │ 改后（Cohere API） │
     ├───────────────┼────────────────────────────┼────────────────────┤
     │ 依赖大小      │ PyTorch ~2GB + 模型 ~4.4GB │ cohere SDK ~几MB   │
     ├───────────────┼────────────────────────────┼────────────────────┤
     │ 首次安装      │ 10-30 分钟                 │ < 1 分钟           │
     ├───────────────┼────────────────────────────┼────────────────────┤
     │ 模型加载      │ ~30 秒                     │ 无                 │
     ├───────────────┼────────────────────────────┼────────────────────┤
     │ 17 页编码     │ 10-30 分钟（CPU）          │ ~10-20 秒          │
     ├───────────────┼────────────────────────────┼────────────────────┤
     │ 每页向量数    │ ~755                       │ 1                  │
     ├───────────────┼────────────────────────────┼────────────────────┤
     │ Zilliz 存储量 │ ~12,835 条/17页            │ 17 条/17 页        │
     ├───────────────┼────────────────────────────┼────────────────────┤
     │ 检索精度      │ ColBERT 多向量（高）       │ 单向量（够用）     │
     └───────────────┴────────────────────────────┴────────────────────┘

     验证方法

     1. pip install -r requirements.txt 确认秒装完成
     2. python app.py 启动 Gradio
     3. 上传 PDF → 编码 → 确认 Zilliz 中每页 1 条 1024 维向量
     4. 提问 → 确认检索到相关页面 → 确认 LLM 返回回答
